# CSV Reformat

## How to use

```
Usage of ./csv-format:
  -d string
        -d <delimiter> (default ":")
  -f    add -f to format file and save to input path
  -i string
        -i <file_path>
  -o string
        -o <file_path> (default "out.csv")
```
